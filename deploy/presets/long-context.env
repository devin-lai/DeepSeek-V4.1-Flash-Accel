# Long prompts. KV cache dominates, so concurrency comes down and utilisation
# goes up. Measured 12 811 total tok/s on 16K inputs at 4 streams.
source "$(dirname "${BASH_SOURCE[0]}")/default.env"
MAX_LEN=262144
MAX_SEQS=8
GPU_UTIL=0.93
