# Exact prefill staging on the measured DSpark-5 latency configuration.
# Requires vllm-dsv41-opt >=0.3.0 and the pinned stack/patches.
# Three-trial comparison: benchmarks/results/2026-09-15-staging-and-batching.md.
source "$(dirname "${BASH_SOURCE[0]}")/v41-flash-latency.env"
MARLIN_STAGE_MIN_TOKENS=256
