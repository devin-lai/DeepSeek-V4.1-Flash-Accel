# Maximise aggregate tokens/s for batch work. Trades first-token latency and
# leaves less headroom for long prompts.
source "$(dirname "${BASH_SOURCE[0]}")/default.env"
GPU_UTIL=0.93
MAX_SEQS=64
MAX_LEN=32768
